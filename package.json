{
  "name": "wheel-strategy-screener",
  "version": "0.2.8",
  "private": true,
  "description": "End-of-week CSP and covered-call screener using TradingView data and a local Robinhood Chrome helper.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test \"test/*.test.js\"",
    "test:smoke": "node --test test/smoke.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
